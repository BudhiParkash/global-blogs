# GlobalsBlog — robots.txt

# Allow the AdSense content crawler to read every page
# so Google can match relevant ads to your content.
User-agent: Mediapartners-Google
Allow: /

# Allow all other crawlers
User-agent: *
Allow: /
Disallow: /api/
Disallow: /search

# Sitemap
Sitemap: https://globalsblog.com/sitemap-index.xml
